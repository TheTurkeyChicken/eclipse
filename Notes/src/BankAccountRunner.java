
public class BankAccountRunner {
	public static void main(String[] args) {
//		SavingsAccount josh = new SavingsAccount();
		/* BankAccount jack = new BankAccount();
		BankAccount adam = new BankAccount();
		BankAccount michael = new BankAccount();
		
		SavingsAccount allen = new SavingsAccount();
		allen.deposit(0.01);
		allen.setBalance(10);
		allen.calculateInterest(1.0, 1);
		allen.premiumSavings();
		
		// Jack "is-a" bank account and does not have access to SavingsAccount methods
		// jack.premiumSavings(); is an error
		
		// allowed
		BankAccount connor = new SavingsAccount();
		
		// rule:
		// connor cannot run SavingsAccoutn methods unless they are overwritten by SavingsAccount
		
		// connor.premiumSavings();
		// cannot run because premiumSavings is in SavingsAccount
		// but NOT in BankAccount
		
		connor.deposit(0.5); // can access BankAccount methods
		
		connor.calculateInterest(1.0, 1);
		// allowed because calculateInterest was overwritten by SavingsAccount
	
		// to call a method, use dot
		jack.deposit(5.25);
		jack.deposit(1.15);
		jack.deposit(0.25);
		adam.deposit(10.20);
		System.out.println(jack.getBalance());
		
		double jackinterest = jack.calculateInterest(0.05, 1);
		System.out.println("Jack\'s interest is " + jackinterest);
		
		System.out.println(michael); // prints hexadecimal
		
		// variable scope
		// where a variable is valid only between { }
		*/
	}
}
